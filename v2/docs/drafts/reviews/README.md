# Вердикты модератора

Модель: GPT-5.6 Terra Medium. Промпт: [PROMPT.md](PROMPT.md). Канон: [RECIPE.md](../../RECIPE.md).

Оркестратор не даёт Terra ходить по репозиторию: в Task только текст промпта и JSON рецепта.

Импорт в БД **не** читает файлы из этой папки. Вердикт — в чате (или сюда по желанию человека). В Postgres: `import_draft --path`.
