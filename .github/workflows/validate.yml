name: Validate recipes

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate archive/v1/data/recipes/
        run: node archive/v1/scripts/validate-recipes.js

      - name: Validate archive/v1/data/tips.json
        run: node archive/v1/scripts/validate-tips.js

      - name: Validate reference JSON
        run: node archive/v1/scripts/validate-reference.js
